name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-please:
    name: Release PR / GitHub release
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      - uses: googleapis/release-please-action@v5
        id: release
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  print-kit:
    name: Attach print kit to the release
    needs: release-please
    if: needs.release-please.outputs.release_created == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Install project
        run: uv sync --locked --no-dev

      # Сборка 2021 года из apt не знает движка manifold и режет гравировку
      # логотипа минутами; свежий AppImage делает то же за секунды.
      - name: Install openscad
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            fonts-dejavu-core libgl1 libglu1-mesa libegl1 libopengl0 \
            libfontconfig1 libfreetype6 libharfbuzz0b libdouble-conversion3 \
            libxkbcommon0 libxkbcommon-x11-0 libxcb1 libxcb-cursor0 libxcb-icccm4 \
            libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 \
            libxcb-shape0 libxcb-sync1 libxcb-xfixes0 libxcb-xinerama0 libdbus-1-3
          curl -sSLo "$RUNNER_TEMP/openscad.AppImage" \
            https://files.openscad.org/snapshots/OpenSCAD-2026.06.12-x86_64.AppImage
          chmod +x "$RUNNER_TEMP/openscad.AppImage"
          # Распаковываем, а не монтируем: FUSE на раннере нет.
          (cd "$RUNNER_TEMP" && ./openscad.AppImage --appimage-extract >/dev/null)
          echo "OPENSCAD=$RUNNER_TEMP/squashfs-root/AppRun" >> "$GITHUB_ENV"
          "$RUNNER_TEMP/squashfs-root/AppRun" --version

      - name: Build everything for print
        run: uv run ubt-race-docs all --with-trophies --out dist

      - name: Pack the whole kit
        run: zip -j -r "ubt-tt-print-kit-${TAG}.zip" dist
        env:
          TAG: ${{ needs.release-please.outputs.tag_name }}

      - name: Attach to the release
        run: gh release upload "$TAG" dist/* "ubt-tt-print-kit-${TAG}.zip" --clobber
        env:
          TAG: ${{ needs.release-please.outputs.tag_name }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
